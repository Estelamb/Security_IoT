name: Deploy Documentation

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write  # Required for branch operations
  pages: write
  id-token: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        persist-credentials: false

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
          python -m pip install --upgrade pip
          pip install sphinx sphinx-rtd-theme sphinx-copybutton sphinx_simplepdf sphinxcontrib-mermaid
          pip install streamlit pytz numpy pandas scikit-learn pm4py paho-mqtt

    - name: Build documentation
      working-directory: docs
      run: |
        rm -rf build/
        sphinx-build -b html source build
        touch build/.nojekyll  # Critical for GitHub Pages

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: docs/build
        keep_files: false
        force_orphan: true  # Clean branch history