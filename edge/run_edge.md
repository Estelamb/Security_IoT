# Follow mqtt_broker.md and venv_edge.md to set up the environment and broker before running the anomaly detector.

# Make the launch script executable
chmod +x launch_edge.sh

# Run the Anomaly Detector
./launch_edge.sh